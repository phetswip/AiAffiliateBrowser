/**
 * Flow Dropdown Helper — MAIN WORLD (Backup)
 * สำรองเผื่อ ISOLATED world patch ไม่เพียงพอ
 * 
 * ถ้า dropdown เปิดค้างนาน 2 วินาที → auto-click Create Image
 */

(function () {
    'use strict';

    if (window.__flowDropdownHelper) return;
    window.__flowDropdownHelper = true;

    console.log('[Flow Helper] ✅ MAIN world backup helper loaded');
})();
